const Title = ({ title }) => {
  return (
    <div className="mb-4">
      <h2 className="text-lg font-bold text-slate-600 dark:text-grayLight">
        {title}
      </h2>
    </div>
  );
};

export default Title;

const Title = ({ title }) => {
  return (
    <div className='my-2'>
      <h2 className='font-semibold text-xl text-gray-900 dark:text-white'>
        {title}
      </h2>
    </div>
  );
};

export default Title;
